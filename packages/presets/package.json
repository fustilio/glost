{
  "name": "glost-presets",
  "version": "0.5.0",
  "description": "Preset configurations for common GLOST use cases",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "keywords": [
    "glost",
    "presets",
    "configuration"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "glost-core": "workspace:*",
    "glost-processor": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.3",
    "vitest": "^1.6.0"
  }
}
